# Platform-specific configuration for Road Rescue 360 Backend
# This file helps handle cross-platform compatibility

# Ignore optional dependencies that might fail on different platforms
optional=true

# Use npm ci for faster, more reliable installs
package-lock=true

# Don't install dev dependencies in production
production=true

# Platform-specific package handling
target_platform=linux
target_arch=x64
